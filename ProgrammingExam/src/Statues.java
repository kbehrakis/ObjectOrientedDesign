import java.util.LinkedList;

class Statues implements PlacesToVisit{

	 Location where;  // location on the campus map
	 
	  Statues(Location where) {
			this.where=where;  // location on the campus map
	  }
}
